extends layout

block main
  h1 Configuration is Code Blog
  if collections.posts && collections.posts.length > 0
    each post in collections.posts
      h3
        a(href='/'+post.path)!= post.title
      div!= post.excerpt
  else
    p.
      No blog entries are currently available.
      Try the #[a(href='/about') about page]
      or #[a(href='/author') author bio].
